# Mailpit

Ambiente local para inspeção de e-mails enviados pela API.

- SMTP: `localhost:1025`
- Interface: http://localhost:8025

Suba somente o serviço com `make mailpit` ou junto de tudo com `make up`.
